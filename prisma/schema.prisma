generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model qr_codes {
  id        String   @id
  type      String
  content   String
  qrDataUrl String
  label     String?
  createdAt DateTime @default(now())
}


